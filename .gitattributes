# flutter_rust_bridge 2.12.0 invokes the pinned Freezed generator for FFI
# unions. Its generated `*.freezed.dart` output contains blank lines with two
# spaces; generated files must be committed exactly as emitted, so do not make
# those stable generator bytes fail `git diff --check`.
lib/src/rust/**/*.freezed.dart whitespace=-trailing-space
