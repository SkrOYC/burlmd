artifact: ci-role-evidence.schema.json
surface: json
flows: [flow-release]
boundaries: [BND-16, BND-17]
certainty: assumed
assumption: "Schema version 16 replaces the managed Linux role label with ubuntu-22.04. It retains the role shape, the 4-CPU, 16,000,000,000-byte memory, and 14,000,000,000-byte storage profile, sealing receipt version 2, and the canonical BURL-O001 producer-lineage transport. The diagnostic prerequisite run did not exercise a complete role document."
settles_when: "Role, receipt, and BURL-O001 producer-to-consumer fixtures validate accepted and rejected version 16 documents. An accepted managed BURL-M003 run must carry ubuntu-22.04 role evidence through candidate capture, fresh sealing, and aggregate validation. The fixtures also reject a receipt-artifact observation inside the version 2 receipt, validate signed producer-receipt createdAt and expiresAt values, verify the exact lineage SHA-256, and require downloadedStageArtifactId to equal producerLineage.stageArtifact.artifactId."
