artifact: ci-role-evidence.schema.json
surface: json
flows: [flow-release]
boundaries: [BND-16, BND-17]
certainty: assumed
assumption: "Schema version 15 and sealing receipt version 2 define candidate guards plus the canonical BURL-O001 producer-lineage transport. Raw contract version 37 keeps both versions byte-identical and carries Linux capacity as a separate wrapper-owned sealed member. The receipt deliberately contains no observation of its own post-upload artifact. Implementation tests have not exercised this contract."
settles_when: "Role, receipt, and BURL-O001 producer-to-consumer fixtures validate accepted and rejected version 15 documents, reject a receipt-artifact observation inside the version 2 receipt, validate signed producer-receipt createdAt and expiresAt values, verify the exact lineage SHA-256, and require downloadedStageArtifactId to equal producerLineage.stageArtifact.artifactId. Linux BURL-O001 fixtures must also reject linux-capacity.ndjson as an internalArtifacts member and preserve the separate sealed transcript."
