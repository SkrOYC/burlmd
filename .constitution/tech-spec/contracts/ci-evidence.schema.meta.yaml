artifact: ci-evidence.schema.json
surface: json
flows: [flow-release]
boundaries: [BND-15, BND-16, BND-17]
certainty: assumed
assumption: "Aggregate schema version 20 replaces only the managed Linux candidate and sealing labels with ubuntu-22.04. It retains seal-only hosted origin, the attested canonical BURL-O001 lineage, receipt action-to-REST observations, and the caller transport REST-to-download observation. The diagnostic prerequisite run did not exercise aggregate evidence."
settles_when: "Final aggregation fixtures validate accepted version 20 evidence. An accepted managed BURL-M003 run must bind the ubuntu-22.04 candidate and sealing observations to role schema version 16. The fixtures acquire the caller transport by immutable artifact ID, verify the raw downloaded archive before extraction, retain receiptDigestTransportArtifact, require every sealingReceiptArtifact REST digest to equal sha256: plus the transport's uploadActionDigest, validate downloadedStageArtifactId equality with producerLineage.stageArtifact.artifactId, and exercise every typed candidate-guard, transport-corruption, transport-substitution, producer-receipt freshness, canonical-lineage, and compatibility-stage rejection."
