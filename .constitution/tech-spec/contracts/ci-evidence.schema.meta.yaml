artifact: ci-evidence.schema.json
surface: json
flows: [flow-release]
boundaries: [BND-15, BND-16, BND-17]
certainty: assumed
assumption: "Aggregate schema version 19 defines seal-only hosted origin, the attested canonical BURL-O001 lineage, receipt action-to-REST observations, and a caller transport REST-to-download observation that implementation tests have not exercised."
settles_when: "Final aggregation fixtures validate accepted version 19 evidence. They acquire the caller transport by immutable artifact ID, verify the raw downloaded archive before extraction, and retain receiptDigestTransportArtifact. They require each sealingReceiptArtifact REST digest to equal sha256: plus the transport's uploadActionDigest, validate downloadedStageArtifactId equality with producerLineage.stageArtifact.artifactId, and exercise every typed candidate-guard, transport-corruption, transport-substitution, producer-receipt freshness, canonical-lineage, and compatibility-stage rejection."
