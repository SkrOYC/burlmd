artifact: provisional-spikes.toml
surface: cli
flows: [flow-release]
boundaries: [BND-15, BND-16, BND-17]
certainty: assumed
assumption: "Raw contract version 33 defines the OD-11 authority split and the attested canonical BURL-O001 producer-lineage transport, but implementation tests have not exercised it."
settles_when: "BURL-M003 and BURL-O001 contract fixtures exercise version 33, including every candidate guard, producer-receipt freshness check, canonical-lineage byte check, downloadedStageArtifactId equality with producerLineage.stageArtifact.artifactId, and compatibility-stage rejection path."
