artifact: provisional-spikes.toml
surface: cli
flows: [flow-release]
boundaries: [BND-15, BND-16, BND-17]
certainty: assumed
assumption: "Raw contract version 34 defines the OD-11 authority split, the attested canonical BURL-O001 producer-lineage transport, the fresh-dispatch-only lifecycle, and the trusted caller transport for each sealing receipt upload digest."
settles_when: "BURL-M003 and BURL-O001 contract fixtures exercise version 34. They pin the two receipt outputs on every reusable role workflow, map all six values into the caller receipt_digests job, validate the version 1 receipt digest transport, and reject any missing, extra, duplicate, malformed, reordered, wrong-role, wrong-run, wrong-attempt, wrong-signer, wrong-nonce, wrong-name, wrong-ID, or action-to-REST digest mismatch. They require aggregate schema version 18 to retain both digest forms in every origin.sealingReceiptArtifact. Existing fixtures continue to exercise the 26-field BURL-O001 interface, exact lineage, fresh-dispatch attempt guards, immutable trusted controls, exact 11-artifact ordinary and 13-artifact BURL-O001 inventories, overwrite: false, and the no-deletion policy."
