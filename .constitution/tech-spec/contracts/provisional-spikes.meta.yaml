artifact: provisional-spikes.toml
surface: cli
flows: [flow-release]
boundaries: [BND-15, BND-16, BND-17]
certainty: assumed
assumption: "Raw contract version 33 defines the OD-11 authority split, the attested canonical BURL-O001 producer-lineage transport, and all four compatibility-stage seal and consumer helpers as trusted controls, but implementation tests have not exercised the contract or its complete control inventory."
settles_when: "BURL-M003 and BURL-O001 contract fixtures exercise version 33, assert that scripts/prepare-compatibility-stage.sh, scripts/record-compatibility-stage-rest.sh, scripts/write-compatibility-stage-lineage.sh, and scripts/prepare-compatibility-stage-consumer.sh are immutable trusted controls, reject byte or mode drift in each helper, and exercise every candidate guard, producer-receipt freshness check, canonical-lineage byte check, downloadedStageArtifactId equality with producerLineage.stageArtifact.artifactId, and compatibility-stage rejection path."
