artifact: provisional-spikes.toml
surface: cli
flows: [flow-release]
boundaries: [BND-15, BND-16, BND-17]
certainty: assumed
assumption: "Raw contract version 35 defines the OD-11 authority split, the attested canonical BURL-O001 producer-lineage transport, the fresh-dispatch-only lifecycle, and the verified caller transport for each sealing receipt upload digest."
settles_when: "BURL-M003 and BURL-O001 contract fixtures exercise version 35. They pin the two receipt outputs on every reusable role workflow and map all six values into the caller receipt_digests job. They acquire that transport by immutable artifact ID and verify its raw archive against the canonical REST digest before extraction. Corruption and substitution fixtures reject before JSON parsing. The fixtures validate the version 1 transport data and reject any missing, extra, duplicate, malformed, reordered, wrong-role, wrong-run, wrong-attempt, wrong-signer, wrong-nonce, wrong-name, wrong-ID, or action-to-REST digest mismatch. They require aggregate schema version 19 to retain receiptDigestTransportArtifact and both digest forms in every origin.sealingReceiptArtifact. Existing fixtures continue to exercise the 26-field BURL-O001 interface, exact lineage, fresh-dispatch attempt guards, immutable trusted controls, exact 11-artifact ordinary and 13-artifact BURL-O001 inventories, overwrite: false, and the no-deletion policy."
