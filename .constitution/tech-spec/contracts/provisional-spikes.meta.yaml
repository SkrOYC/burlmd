artifact: provisional-spikes.toml
surface: cli
flows: [flow-release]
boundaries: [BND-15, BND-16, BND-17]
certainty: assumed
assumption: "Raw contract version 33 defines the OD-11 authority split, the attested canonical BURL-O001 producer-lineage transport, the macOS 15 trusted workflow wrapper's static-shape validation and runtime value-validation order, and all five compatibility-stage seal and consumer helpers as trusted controls. The BURL-M003 and BURL-O001 Stage 4 Tasks artifacts are downstream-stale and require separate adaptation before implementation."
settles_when: "BURL-M003 and BURL-O001 contract fixtures exercise version 33, assert that scripts/prepare-compatibility-stage.sh, scripts/record-compatibility-stage-rest.sh, scripts/write-compatibility-stage-lineage.sh, scripts/prepare-compatibility-stage-consumer.sh, and scripts/validate-compatibility-stage-interface.sh are immutable trusted controls, reject byte or mode drift in each helper, use duplicate-aware source inspection to pin the exact ordered 26 producer-output declarations, consumer-input declarations, and observable caller mappings while rejecting missing, extra, duplicated, or reordered source keys, require the macOS 15 trusted workflow wrapper to invoke the interface validator before any compatibility artifact acquisition or consumer processing, require all 26 runtime values to be nonempty for BURL-O001 and all 26 to be empty for every other ticket, run all three offline attestation-verification commands before the consumer helper consumes their result files and runtime values, and exercise every candidate guard, producer-receipt freshness check, canonical-lineage byte check, downloadedStageArtifactId equality with producerLineage.stageArtifact.artifactId, and compatibility-stage rejection path. Before implementation, a separate Stage 4 planning-engineering-execution adaptation revises the downstream-stale BURL-M003 and BURL-O001 Tasks artifacts; this Stage 3 pass does not change Task scope."
