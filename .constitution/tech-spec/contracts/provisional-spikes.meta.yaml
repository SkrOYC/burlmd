artifact: provisional-spikes.toml
surface: cli
flows: [flow-release]
boundaries: [BND-15, BND-16, BND-17]
certainty: assumed
assumption: "Raw contract version 36 preserves version 35's authority, lineage, lifecycle, and receipt transport. It replaces the copied Linux closure store with an exact read-only closure view and gives only BURL-O001 sparse private Nix state for build outputs."
settles_when: "BURL-M003 and BURL-O001 contract fixtures exercise version 36. Both fixtures must prove exact manifest-to-view equality, hidden ambient store paths, read-only host closure members, reference completeness, working loaders and runtime search path dependencies, disabled nested user namespaces, ARG_MAX headroom, allocated-capacity headroom, and cleanup. BURL-O001 must also prove exact database subset import, persistent private outputs, absent host state and daemon access, and host closure immutability. Existing version 35 receipt transport, 26-field compatibility interface, fresh-dispatch, artifact inventory, and no-deletion fixtures remain required. Aggregate schema version 19, role schema version 15, and spike-result schema version 20 remain byte-identical."
