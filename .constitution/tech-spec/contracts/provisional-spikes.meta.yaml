artifact: provisional-spikes.toml
surface: cli
flows: [flow-release]
boundaries: [BND-15, BND-16, BND-17]
certainty: assumed
assumption: "Raw contract version 36 preserves version 35's authority, lineage, lifecycle, receipt transport, schemas, and artifact inventories. ADR-0020 adds only the assumed exact read-only BURL-M003 closure view needed for the standard Linux runner."
settles_when: "BURL-M003 contract fixtures exercise version 36. They must prove exact manifest-to-view equality, hidden ambient store paths, read-only host closure members and store base, working loaders and runtime search path dependencies, forbidden-client absence, disabled nested user namespaces without nesting, complete-vector ARG_MAX headroom, the start-space guard, descriptor closure, and teardown. Existing version 35 receipt transport, 26-field BURL-O001 compatibility interface, fresh-dispatch, exact 11-artifact ordinary and 13-artifact BURL-O001 inventories, and no-deletion fixtures remain required. Aggregate schema version 19, role schema version 15, spike-result schema version 20, sealing receipt version 2, and receipt transport version 1 remain unchanged."
