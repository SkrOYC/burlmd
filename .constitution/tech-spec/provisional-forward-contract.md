# Provisional forward contract

## Purpose

This document prevents the research wave from becoming an accidental implementation wave. Structured PRD v2.0.5 and Architecture v2.1.2 are binding. The delivered v1.6.x physical contracts remain valid only for already-delivered behavior. Where they conflict with the forward constitution, the forward requirement is authoritative.

## Decision gates

| Forward area | Current physical status | Evidence or decision gate | Final Stage 3 output |
| :--- | :--- | :--- | :--- |
| Desktop preferences and Workspace sessions | Versioned JSON schemas specified for Epic G exceptions | No research Spike; implementation verifies Platform storage APIs and migration behavior | Device-preference schema, per-Workspace session schema, restore and serial-close FFI |
| Canonical Note and Workspace trees | Reduced `AstNode` projection | SPK-BURL-H001 | Canonical document schema, render projection, editing and indexing adapters, revised ADR-007 and FFI |
| Cross-platform paths and conformance repair | Title-verbatim host paths; invalid Notes currently indexed | SPK-BURL-H002 | Path grammar, migration/repair plan, preflight/live-monitor contracts, revised OKF contract and schema |
| Live Workspace monitoring | Manual Rescan only | Final verification of `notify` 8.2.0 and fallback behavior | Observer event contract, debouncing, revision checks, external-change decisions |
| Local history, undo, find, backlinks | Some planned FFI, incomplete production wiring | Canonical AST and path contracts first | Reconciled state and FFI contracts using authoritative Note sessions |
| Atomic Export | Brownfield contract permits partial output | AST, path, and asset closure contracts first | Copy and `.okf` schemas, stable-revision lease, collision and atomic publication contracts |
| Local assets and Object Store | No physical contract | SPK-BURL-I001 plus measured PRD/Architecture review | `BND-06` Local Asset Store model, `BND-11` transfer and verification contract, `BND-21` Object Store configuration and operation contract, identity and key format, hydration, and retention state machine; `BND-14` Provider and `BND-20` Remote don't own Object bytes |
| Private GitHub connection | Superseded OAuth redirect and marker-based merge | ADR-017 plus SPK-BURL-L001 | `BND-14` Provider device authorization, repository selection and provisioning, and eligible `BND-20` Remote location; `BND-20` authenticated history and ref contract; typed `BND-10` analysis and decision schemas; credential adapter; no Object transfer or storage responsibility |
| Releases and updates | Development builds plus committed Epic G headless capture | SPK-BURL-O001 for package choices; BURL-M003 for managed validation bootstrap | Credential-free candidate bundles with noncryptographic runtime guards, fresh-seal provenance, a seal-owned authenticated BURL-O001 compatibility stage, an attested canonical producer-lineage transport that binds producer-receipt freshness, accepted or rejected chain-verifying aggregation, update metadata, and the installed-app release matrix |
| Platform chrome | PR #11 presentation prototype leaked into production | Settled product decision | Remove preference/state/rendering/copy/tests and regenerate visual evidence; no replacement window-frame abstraction |

The release control surface treats the following scripts as immutable trust-anchor controls:

- `scripts/write-receipt-digest-observation.sh`
- `scripts/prepare-compatibility-stage.sh`
- `scripts/record-compatibility-stage-rest.sh`
- `scripts/write-compatibility-stage-lineage.sh`
- `scripts/prepare-compatibility-stage-consumer.sh`
- `scripts/validate-compatibility-stage-interface.sh`

Each reusable role workflow exposes its receipt upload artifact ID and bare digest through exact workflow outputs. The caller's fresh `receipt_digests` job maps all six values into one identity-bound transport without downloading candidate bytes.

Local collection resolves the transport's immutable artifact ID from the exact run inventory. It downloads the raw archive by ID and compares its SHA-256 with the canonical REST digest before extraction. The aggregate retains this check in `receiptDigestTransportArtifact`. Collection then validates the transport and checks each receipt ID and name against REST. It requires each receipt's REST digest to equal `sha256:` plus the transported action digest. The aggregate retains both receipt digest forms in `origin.sealingReceiptArtifact`. The sealing receipt remains version `2` and contains only pre-upload facts. Transport verification detects corruption or substitution but doesn't provide provenance independent of GitHub's artifact service.

Static workflow-shape fixtures use duplicate-aware source parsing to pin the exact ordered 26 producer-output keys, consumer string-input keys, and observable trusted mappings. They also pin every canonical hyphenated consumer input through its uppercase environment variable into the validator's ordered 26-field inventory. Before caller mapping, the macOS 26 output boundary normalizes the three producer sealing aliases for every ticket other than `BURL-O001`. It doesn't rely on omitted reusable-workflow input defaults.

Before acquisition or consumer processing, the macOS 15 trusted workflow wrapper invokes `scripts/validate-compatibility-stage-interface.sh`. Accepted nonempty `BURL-O001` values proceed to all three artifact downloads, offline verification, and `scripts/prepare-compatibility-stage-consumer.sh`. Accepted all-empty values for every other ticket skip only those compatibility steps, then continue the ordinary candidate path. The runtime shell can't validate source key shape or arbitrary value provenance. `scripts/prepare-compatibility-stage-consumer.sh` validates the result files. Downstream checks validate the values it consumes. The script then removes credentials and exposes only verified read-only members to the macOS 15 candidate.

In-workflow validators require literal `GITHUB_RUN_ATTEMPT=1` before relevant work. Local collection requires literal `--attempt 1` and REST `.run_attempt == 1` before acquisition. It re-fetches that exact REST object and requires numeric `.run_attempt == 1` immediately before atomically publishing an accepted report. Local collection does not inspect `GITHUB_RUN_ATTEMPT`. The launcher uses PR #15's `od -An -N16 -tx1 /dev/urandom | tr -d ' \n'` mechanism to generate each 128-bit nonce. It fails closed without a fallback unless the result is exactly 32 lowercase hexadecimal characters. A failed, cancelled, timed-out, or rejected report requires a fresh `workflow_dispatch` run with a new nonce and workflow run ID. GitHub UI and API reruns are forbidden. A recording fake API fixture proves that every path in the complete `trusted_control_paths` inventory makes no artifact-deletion request. Static fixtures require the exact 11-artifact ordinary and 13-artifact BURL-O001 inventories and `overwrite: false` on every upload. A byte or mode change in any helper requires reviewed trust-anchor rotation and evidence-only completion. The local client observes a dispatched workflow for at most 21,600 seconds. That bound does not change role-job or candidate-session timeouts, retries, topology, permissions, or cache trust. It is based on an extrapolation from one measured Linux bootstrap, not an observed end-to-end or Darwin duration.

ADR-020 and raw contract version `40` define two exact read-only Bubblewrap views for `BURL-M003`. Five base sessions receive the 488-member base closure. Only the two integration sessions receive the 547-member integration closure, which adds exact unwrapped Sway and `swaymsg` requisites. Bubblewrap remains the sole candidate namespace and session-payload launch and never enters either view.

Each session commits to its complete effective Bubblewrap argv. The canonical encoding writes the exact UTF-8 bytes of `argv[0]` and every following argument, with one NUL after each value. It covers every namespace, capability, environment, directory, symlink, bind source and destination, selected manifest member, supervisor argument, and session command. Fresh sealing reconstructs these bytes from trusted contract version `40`, the full selected manifest, and retained canonical authority and current-path rows. Each reconstruction resolves all 22 dynamic source identifiers, plus `xdg-runtime` for an integration session, with zero unresolved identifiers. Static and fresh-seal fixtures reject every addition, omission, reordering, duplication, or substitution, including extra capabilities and host-root alias binds.

Each session also creates an authority-backed contract leaf from its frozen `session-contract-root` parent. The complete argv creates `/contract` and mounts that leaf read-only after the session-root bind and before the prepared-root bind. The leaf contains only the exact mode-0444 `locked-nix-closure.sources` file that preflight reads. Fixtures reject missing, writable, substituted, linked, duplicated, incorrectly named, or extra content and any mount-order, source, destination, or retained-authority mismatch.

Only the two integration sessions append `XDG_RUNTIME_DIR`, so only those sessions receive an `xdg-runtime` authority and leaf. Each complete argv mounts the matching current leaf writable at `/candidate/xdg/runtime`; fixed directory creation doesn't create that destination. The source must have the frozen ownership, mode `0700`, empty initial contents, unchanged mount and device identity, and no symbolic link. Teardown removes its sockets, lock, and leaf before the next session. Fixtures reject an omitted or read-only bind, a wrong source or destination, a mode-0755 directory fallback, a substituted or reused leaf, and precreated socket or other content. Base sessions have no runtime leaf, mount, socket, or Wayland variable.

The reduced serializer golden uses an explicitly synthetic `/work` source map and produces 256 arguments, 5,614 bytes, and SHA-256 `cb4b1e128d3f6bd2d4ba6b51854f82ec998bb09a21e294dfa2100fce1b1c7976`. It covers every argument category, including the exact Bash source launch, but isn't a production integration argv. Separate fixtures construct all seven vectors with the actual 488-member or 547-member manifest. Production launch uses the resolved absolute trusted workflow checkout, tested-source checkout, `RUNNER_TEMP`, and role-output paths. PR #15 derives the trusted-control root from the immutable launcher path and passes the tested-source checkout as an explicit role argument. Before launch, local Git checks bind those two roots to the expected workflow-signer and tested-source commits with configuration and hooks disabled. Existing authority and current-path rows retain both roots separately from `RUNNER_TEMP` and the role-output root. Fresh sealing validates those rows structurally and for internal consistency. It doesn't claim independent cryptographic knowledge of paths on the departed candidate host. The complete synthetic golden has 163 authority rows and 163 current-path rows. Its authority SHA-256 is `a84bfa98dbb926d0c4e03228d4291ae0586a770fe8ba9a64984d4f6e390152c6`. Production derives its count from its retained inventory. The environment's OpenSSL link entry is `NIX_LDFLAGS_x86_64_unknown_linux_gnu`, and the final candidate boundary rejects generic `NIX_LDFLAGS`.

The closure view retains source-distinct staging, exact source identity, hidden Nix state, same-path member binds, the store-backed `/usr/bin/env` symlink, descriptor handshake, and capacity authority. A byte count, fixed-flag digest, set comparison, successful parse, or successful execution doesn't bind the invocation.

Two pinned-Nix reproductions produced identical closures. The base has 488 paths, 30,717 manifest bytes, SHA-256 `127043afe260d7756ee6cbda03e39a5bfceb4f7f79ae3a5be1595e447ce15e64`, 6,184,635,112 summed NAR bytes, and 66,314 bind bytes. The integration closure has 547 paths, 34,315 manifest bytes, SHA-256 `353e927fb857fe8f8fc213da4ac53e0a12147ead779e2e29289c6a3034415896`, 6,338,147,240 NAR bytes, and 74,100 bind bytes. The exact unwrapped compositor subset has 195 paths, 11,865 manifest bytes, SHA-256 `d97a41799b1aecc670e31bfd58b339d748498be2bce09d877a0f1a9ed1c6e673`, 649,324,904 NAR bytes, and 25,680 bind bytes. The Bubblewrap parent remains eight paths, 480 manifest bytes, SHA-256 `398d11c9cd9249369cbb18d36661014eafef5ac18ff7adeb076c2c51ef0141fd`, 40,679,016 NAR bytes, and 1,040 bind bytes.

The trusted parent keeps Bubblewrap in its separate eight-path tool closure. It writes the Bubblewrap, compositor, base, and integration manifests below canonical `RUNNER_TEMP` as `burlmd-m003/trusted-parent-bubblewrap.manifest`, `burlmd-m003/unwrapped-compositor-closure.manifest`, `burlmd-m003/base-session-closure.manifest`, and `burlmd-m003/integration-session-closure.manifest`. These host source paths are distinct from namespace destinations. The `/contract` namespace mount contains only the selected `locked-nix-closure.sources` snapshot. The parent proves the exact canonical store path, `bubblewrap 0.11.2` output, executable SHA-256, and tool-closure manifest SHA-256. For each candidate namespace and session payload, it calls only Bubblewrap, directly with an empty process environment. It doesn't directly launch Sway, `swaymsg`, the candidate, or another session payload. The exact trusted preparation, version and identity checks, and pinned post-session util-linux `flock` verifier remain required. This distinction doesn't define a general parent-process allowlist.

The binding Linux sequence retains the exact seven ordered session IDs and descriptor handshake. Every invocation uses the exact raw contract version `40` vector. Before launch, the parent creates and retains an `O_RDWR` descriptor for the original teardown-lock inode. The Bubblewrap branch closes that descriptor before `execve`. The in-namespace preflight and candidate don't execute Bubblewrap.

Base sessions mount no compositor member or runtime leaf, expose no Wayland variable or socket, and run no compositor process. Only the two integration sessions start Sway and the candidate as untrusted siblings under the trusted in-namespace supervisor. Sway uses `env -i`, the headless backend, pixman renderer, and exact launch arguments. `scripts/managed-sway.conf` is a Git-tracked mode-`0644` file. The launcher verifies its exact 72 bytes and SHA-256 `dfb19c5d5cd33e3e2ba7570511cee6c96222a94f1a717886bbbaa7d91dd1ab8a`, then mounts it read-only without claiming a visible mode change. The configuration starts with `swaybg_command -`, disables Xwayland, and contains no include or command execution. Sway creates `/candidate/xdg/runtime/wayland-1` inside the authority-backed leaf. The candidate receives that basename and runtime path, not a parent socket. No `swaybg` process or output is allowed.

The Bash supervisor receives the environment declared by Bubblewrap, then uses the exact Coreutils 9.11 `env -i --` executable as the final candidate environment boundary. It passes 33 resolved fixed and derived assignments in contract order. Integration sessions append four assignments for a total of 37. Direct-entry fixtures inject hostile `PWD`, `SHLVL`, `_`, and canary values and reject every leak or variant. Pinned script interpreters can create their own runtime variables after entry; fixtures record those values and don't describe them as globally absent. The supervisor file starts with `set -euo pipefail` and contains constants, helpers, and one complete `main` function. Pinned Bash 5.3p9 launches it only with `-c 'source "$1"; shift; main "$@"'`, `_`, the trusted script path, and the existing supervisor arguments. Source reaches end of file before `main`; `main` defensively closes descriptor 255 before the first exact inventory. Every branch then `exec`s or exits without another script read. A pinned fixture rejects direct-file or changed-argv launch, proves source failures don't invoke `main`, and preserves `BASH_SOURCE`, standard input, standard output, and standard error. The preflight claim doesn't rely on `CLOEXEC`.

The supervisor signals the candidate process group and waits for its direct candidate child. It also stops and waits for its direct Sway child on success, candidate failure, early Sway failure, timeout, socket disruption, and interruption. Bubblewrap's namespace PID 1 owns and reaps adopted descendants. The supervisor verifies that the Sway PID no longer exists and that no `swaybg` process or output remains. It closes supervisor log descriptors 3 through 6, then opens `/candidate/session` as temporary descriptor 3 with no-follow directory flags. The device, inode, UID, and GID must match the session-root identity recorded before candidate execution. Only then does `openat` create and fsync `cleanup.frame`. The parent validates that record and acknowledges namespace exit. The supervisor removes the handshake files through descriptor 3, closes it, and requires only descriptors 0 through 2 before exit. The cleanup descriptor never reaches candidate execution. After the outer process exits, the parent's retained-lock acquisition proves that namespace PID 1 finished reaping before the parent appends the session frame. Candidate output, Sway, `swaymsg`, and their sockets remain untrusted. Only fresh sealing is authoritative.

Each preflight begins with candidate standard streams fixed as descriptors 0 through 2. The Bubblewrap branch first closes the parent-retained teardown descriptor. Descriptor 3 is the preflight-record write end, and descriptor 4 is the acknowledgement read end. After source reaches end of file, `main` defensively closes descriptor 255. The first exact inventory therefore contains only descriptors 0 through 4. After acknowledgement, every branch closes both handshake descriptors and reverifies the standard streams. The trusted supervisor then raises and verifies loopback. A base branch requires only descriptors 0 through 2 and immediately performs the final Coreutils `env` exec. An integration supervisor may then open only descriptors 3 through 6 for the exact Sway and readiness output logs. Its forked candidate closes every descriptor above 2, requires only the unchanged standard streams, and reverifies them immediately before the final Coreutils `env` exec. Fixtures reject descriptor 255, the teardown descriptor, and every other leak, swapped log, substituted target, wrong access mode, and standard-stream replacement.

After the outer Bubblewrap process exits, the parent fstats the retained teardown descriptor and requires its original device and inode. It invokes pinned util-linux `2.42` as `flock --fcntl --exclusive --timeout 5 --conflict-exit-code 73 TEARDOWN_LOCK_FD`. In this command, `TEARDOWN_LOCK_FD` is the retained descriptor number. This OFD write lock conflicts with Bubblewrap's POSIX read lock while the namespace reaper lives. A timeout or any nonzero result fails the session. Candidate unlink or replacement of the writable pathname can't change the retained inode. After acquisition, the parent removes all ephemeral leaves while holding the OFD lock, then unlocks and closes the descriptor. No separate read-only lock mount is required.

The loopback check runs inside the outer network namespace before any candidate, Sway, or `swaymsg` process. The outer Bubblewrap invocation remains the containment authority. Its unshared network namespace has no host interfaces, routes, external network, or host network descriptors. Untrusted code can alter its own private network configuration. Bubblewrap is absent from the candidate view. Candidate-created nested user namespaces aren't claimed to be impossible or used as a containment control. The preflight doesn't contain `userns-disabled=true`, and its parser rejects that stale assertion.

After trusted Nix and tool preparation, the BURL-M003 `ubuntu-22.04` role wrapper runs the pinned Bubblewrap loopback probe before tested-source dependency execution. Exact success is a hosted prerequisite. Any failure blocks candidate execution, bundle creation, and upload, and no accepted evidence exists for that run. The wrapper has no sudo fallback, host-policy write, AppArmor profile installation, host-origin authentication addition, second probe after mutation, or restoration machinery. Diagnostic run `34365475868` passed the exact prerequisite on image `20260831.284.1`; it didn't run the complete role or produce accepted evidence. GitHub starts label deprecation on 2026-09-17 and retires the image on 2027-04-17. If the bridge is unavailable, the role fails closed pending another Stage 3 measurement.

For hosted macOS failures, the trusted wrapper preserves the original nonzero status and completes bounded cleanup. It can emit one failure-only diagnostic block. The exhaustive ordered gate-log map contains `flutter-test`, `dart-analyze`, and `cargo-metadata` with their `results/role-step-GATE.log` paths. It then contains the two path-hashed integration IDs for `integration_test/production_host_flow_test.dart` and `integration_test/shell_flow_test.dart`, with their macOS `results/integration-SHA.log` paths. The map does not use the Linux-only `results/role-step-integration-SHA.log` paths. The raw contract defines every exact ID and relative path.

The wrapper opens the selected path read-only with no-follow and nonblocking flags, requires a regular file, observes its size through `fstat`, and copies at most the final 65,536 bytes into one fixed-size snapshot. A source at or below that limit can fit wholly in the bounded snapshot. The wrapper performs no additional or unbounded full-file pass. It hashes and encodes only the snapshot with standard padded Base64 specified by RFC 4648 section 4, without line wrapping. Every output line starts with the exact 24-byte ASCII prefix `burlmd-m003-diagnostic: `, including its single trailing U+0020 space. The metadata field names, ordering, delimiters, and block framing are implementation-defined presentation, not an accepted-evidence wire schema. The fixed gate ID, original exit status, observed source-log size, retained count, and snapshot SHA-256 remain required values. A link, FIFO, socket, device, or directory yields no diagnostic. An unavailable or changing source can suppress the diagnostic. A completed snapshot binds only its retained bytes and makes no complete-log integrity claim. Neither case can alter cleanup or status. The output never becomes a command, accepted evidence member, or artifact.

Only during the BURL-M003 `macos-26-arm64` first integration gate for `integration_test/production_host_flow_test.dart`, the private `xcrun` link becomes an observer. It matches only one invocation: the working directory is the canonical candidate workspace's `macos` directory, and argv is exactly `xcodebuild -clonedSourcePackagesDirPath <candidate-workspace>/build/macos/SourcePackages -resolvePackageDependencies`. The canonical candidate workspace is `candidate_root/workspace` below the explicit candidate-output role argument. Flutter 3.44.3 derives `build/macos` from the unoverridden `build-dir` default and appends `SourcePackages`. A role, gate, working-directory, argv-count, argument, or expected-path substitution is a nonmatch that directly executes the retained real `xcrun`. For a match, the observer starts the real tool once with its received argv, working directory, and environment. Two relay children forward the received stdout byte sequence to stdout and stderr byte sequence to stderr. They retain only the final 32,768 bytes from each stream, can change delivery timing, and cannot establish cross-stream ordering. A tail-retention/storage write failure is capture loss only: its relay must keep draining upstream input and forwarding it downstream. An upstream resolver-pipe read failure or downstream forwarding-write failure is an observer execution failure. The observer returns nonzero first. The enclosing candidate wrapper then cleans up, preserves any already observed candidate status, emits no resolver handoff, and makes no no-observer or unobserved-real-xcrun-status claim. Only a completed relay pair, successful tail retention, and a real xcrun normal exit in 1 through 255 permits the observer to publish bounded tails and the complete three-file handoff before it returns that normal status. A normal exit of 0 publishes no resolver handoff or diagnostic data, even if a later integration step fails. A signal-terminated real xcrun also publishes no resolver handoff or diagnostic data. The observer distinguishes the native wait status, restores or unblocks the child's termination signal as needed, and terminates itself with that same signal. Flutter then observes its child as signal-terminated. The observer never sends the signal to the Flutter parent and must not synthesize 128 plus a signal number. Paired fixtures distinguish normal exit 137, which returns normal 137 and publishes the handoff after successful relays, from `SIGKILL`, for which the observer terminates itself and Dart observes -9 with no resolver handoff or diagnostic data. A tail-retention/storage write failure suppresses capture but the observer still returns the observed normal status. The existing one post-cleanup failure block can include the observed normal nonzero status and the two count, SHA-256, and Base64 tail values. Pinned fixtures use streams exceeding pipe capacity and prove exact and nonmatching behavior, bounded capture, tail-storage continuation, observer-execution-failure behavior, cleanup, and workflow-command safety. Historical run `34313264792` had no retained gate log. Run `34554369301`, attempt 1, retained a 2,167-byte snapshot with SHA-256 `29941e1f9ab09916775f58e55e5326984c10855a87375ca5935d2b6939a221fe`; it identifies a resolver-stage failure but not its inner cause.

The observer uses no inherited diagnostic descriptor or unlinked child file. Before the gate, the wrapper creates one mode-0700 directory below the canonical role runtime root and retains close-on-exec descriptors for that directory and the trusted root. The complete handoff is `resolver.stdout.tail` for raw stdout bytes, `resolver.stderr.tail` for raw stderr bytes, and `resolver.status` for the bounded UTF-8 status record. Each tail is mode `0600` and at most 32,768 bytes. The status record is mode `0600`, at most 512 bytes, and contains exactly ordered LF-terminated `format`, `exit_status`, stdout count and SHA-256, and stderr count and SHA-256 fields. `exit_status` uses `DECIMAL_1_TO_255`: a nonzero digit followed by zero or more digits. Each tail-byte field uses `DECIMAL_0_TO_32768`: `0` or a nonzero digit followed by zero or more digits. The parser range-checks each value to its named inclusive bound. Leading zeros, signs, whitespace, and alternative spellings reject. Only after both relay data paths complete, tail retention succeeds, and the real tool has a normal exit in 1 through 255 can the observer atomically rename a complete temporary status file to `resolver.status` before it returns that normal status. A normal exit of 0 writes no handoff and emits no resolver diagnostic data. A signal-terminated real tool writes no handoff and emits no resolver diagnostic data. The observer uses the locked Perl interpreter's native wait status, restores or unblocks the child's signal as needed, and terminates itself with that same signal. Flutter then observes its child as signal-terminated. The observer never sends the signal to the Flutter parent or returns 128 plus the signal number. A tail-retention/storage write failure suppresses capture but the observer still returns the observed normal status.

After candidate cleanup, the parent opens each declared file descriptor-relatively with read-only no-follow nonblocking flags, immediately validates type, mode, link count, size, and device-and-inode identity with `fstat`, and reads bounded data only from that descriptor. It validates the status bindings and rechecks the same descriptors before output. Before output, descriptor-relative no-follow lookup must retain each file and directory identity. The parent performs no handoff `unlink` or `rmdir`; it leaves the small private transient area for the hosted job lifecycle and never recursively traverses, follows links, or removes an uncertain entry. A same-UID surviving candidate can replace a final name after the last identity check, so pathname removal cannot preserve that identity guarantee. A missing or unsafe handoff suppresses resolver data without changing an already observed candidate status or cleanup. An upstream resolver-pipe read failure or downstream forwarding-write failure is separately observable when the observer returns nonzero before enclosing cleanup and has no counterfactual status guarantee. A tail-retention/storage write failure is capture loss only, and its relay must keep draining and forwarding. This fixed handoff defense doesn't claim arbitrary hosted-macOS candidate lifecycle containment.

Before the first candidate session, the launcher freezes every BURL-M003 capacity authority. Each session has one row for its staging leaf and each of the 22 dynamic argv sources. Each integration session adds one runtime row. Retained tab-separated paths use literal UTF-8 and reject NUL, tab, LF, CR, and backslash bytes at ingestion and parsing. No step can add a parent, mount, device, slot, leaf, writable path, or argv source outside that authority. The existing per-device 4,000,000,000-byte start guard remains a start guard, not a packaging claim.

The Linux role retains one `logs/burl-m003-linux-closure-view.log` through unchanged `roleEvidence.internalArtifacts`. Raw contract version `40` defines its version 2 grammar and golden fixture. The ordered header explicitly includes the four base and integration source-identity count and digest fields already present in the golden. The log retains the pinned verifier identity and, for each session, the original lock device and inode plus stable-identity and acquisition results. It also retains both manifests, full-vector digests, resolved authorities and current paths, preflight, capacity, the Sway configuration hash, and integration cleanup. Its host-policy row records successful first probe and fixed not-applied or not-applicable mutation fields. Fresh sealing reconstructs and validates the contract mount, exact candidate entry environment, descriptor branch, teardown proof, loopback setup, no host-policy write, and absence of `swaybg`. Accepted aggregation uses role schema version `16` and aggregate schema version `20` without adding a field or service artifact.

## Contract-scoped production applicability

The active Epic G M0 production exceptions are `BURL-G001` through `BURL-G005`, `BURL-G007`, and `BURL-M015`. Raw contract version `40` and `BURL-M003` ci_bootstrap are dormant. They permit no bootstrap, recovery, production, or hosted execution. The private Linux-first path has no hosted-execution authority and must not invoke the `BURL-M003` workflow, role, sealing, or aggregation path.

The co-merged Tasks v3.2.14 adaptation is historical. It updates only BURL-M003's raw-version and diagnostic references, and does not authorize BURL-M003. BURL-G011, BURL-P002, and BURL-O012 remain historical adaptations. BURL-O001 remains blocked, but its raw-contract-version-39, PR #37, and Tasks-SHA stop wording is stale. Preserving that stop doesn't make it current or settle it. Its owning later Stage 3 routing and Stage 4 adaptation must replace the wording before BURL-O001 can run. The historical adaptation preserves the gate-log map, exact prefix, canonical decimal grammar, no-removal residual-race policy, BURL-O001 stop, artifact inventories, permissions, evidence classes, tickets, estimates, and epic graph. After the user resumes upstream planning and a later approved Stage 3/4 adaptation authorizes BURL-M003, complete Epic M, including the reviewed `BURL-M003` recovery implementation, fresh accepted managed run, exact three-file evidence pull request, epic closeout, and reconciliation.

The recovery base history retains reviewed BURL-M015 commit `6d30b7445b0108a6a5dd963cd2aa2ae5f5090485` and the original PR #15 anchor `f72659cef4487317c9e984e01f775a358f814a41`. The first implementation uses the reviewed documentation tip after the Stage 4 adaptation as its first parent. This closure needs no self-referential field or placeholder commit.

For the first raw-version-40 recovery, one reviewed implementation commit starts from the reviewed documentation tip after the Stage 4 adaptation. If the recovery later fails under the same contract, each correction is one separately reviewed commit from the immediately preceding reviewed `master` tip. The selected `BASE_SHA` is always the replacement anchor's first parent and retains the merged approved documentation and original lineage. A source-branch documentation SHA cannot be `BASE_SHA`. The unchanged bootstrap allowlist applies only to that one-commit range. Never accept an arbitrary ancestor. Every replacement anchor requires a fresh run, nonce, self-validation, and dedicated reviewed evidence pull request.

Every other production ticket remains blocked until its own decision evidence lands. Product Requirements and Architecture are reviewed when that evidence changes an upstream assumption. Final Stage 3 then replaces that ticket's provisional physical contract, and Stage 4 adapts the ticket before implementation. A ticket doesn't wait for an unrelated Spike, and one completed Spike doesn't authorize neighboring production work.

Spike Tasks may read production code and fixtures but may write only within their prototype roots and report records. Final Stage 3 still owns the reconciled FFI, schemas, bill of materials, repository layout, error taxonomy, and exact implementation commands for each affected contract.

## Known brownfield contradictions

- `ffi_api.rs` describes `AstNode` as a render projection, while the forward model requires a canonical extended AST.
- The OAuth redirect and PKCE surfaces require a client-secret-era design superseded by GitHub App device flow.
- The OKF bundle derives filenames verbatim from titles, which OD-05 explicitly reopens.
- The schema and open-Workspace comments tolerate and index invalid Notes, while the authority model now excludes them until Repair or Exclude.
- `export_workspace` describes partial, non-gating output; forward Export must be object-complete and atomic.
- Git Suggestions assume marker-bearing content conflicts and don't cover Lifecycle or Asset Decisions.
- No physical model exists for Workspace observation, S3-compatible object configuration, asset reachability, or release metadata.
- PR #15 at `9719259f1ecee819af96c98c2be210156f198343` copies the Linux candidate closure into a private store. Its measured prebuild allocation exceeds the standard runner profile. `BURL-M003` must use ADR-020 and raw contract version `40`. `BURL-O001` remains blocked on a later Stage 3 packaging-isolation and capacity decision.

These are tracked inputs to final reconciliation, not defects for a research Task to patch piecemeal.
