#!/usr/bin/env bash

# The devenv CLI emits the direnvrc matching its own version. This is the form
# documented for devenv v1.4+; the older `source_url` recipe pinned a direnvrc
# revision by hand, which drifts from the installed CLI and from devenv.lock.
eval "$(devenv direnvrc)"

use devenv
